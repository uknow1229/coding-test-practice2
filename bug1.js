function doubleArrayValues(array) {
  for (let i = 0; i < array.length; i++) {
    array[i] = array[i] * 2;
  }
  return array;
}

console.log(doubleArrayValues([1, 2, 3, 4, 5])); // Expected output: [2, 4, 6, 8, 10]

// 以下のJavaScriptのコードをご覧ください。このコードは、配列の各要素を2倍にすることを目的としていますが、何か問題があります。
